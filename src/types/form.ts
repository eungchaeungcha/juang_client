export interface LoginFormType {
  id: string;
  password: string;
}

export interface SignupFormType {
  id: string;
  password: string;
}

export interface ProfileFormType {
  charactor: string;
  color: string;
  nickname: string;
}
